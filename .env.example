# Environment Variables Template
# Copy this file to .env and fill in your values
# NEVER commit .env to Git!

# ===========================================
# PostgreSQL Configuration
# ===========================================

# PostgreSQL admin password
POSTGRES_PASSWORD=your-secure-postgres-admin-password-here

# PostgreSQL application user password
POSTGRES_USER_PASSWORD=your-secure-user-password-here

# PostgreSQL database name (default: demodb)
POSTGRES_DATABASE=demodb

# PostgreSQL application username (default: demouser)
POSTGRES_USERNAME=demouser

# ===========================================
# Redis Configuration
# ===========================================

# Redis password
REDIS_PASSWORD=your-secure-redis-password-here

# ===========================================
# Application Configuration (Phase 2+)
# ===========================================

# JWT secret for guest authentication (min 32 chars)
# JWT_SECRET=your-jwt-secret-minimum-32-characters-long

# Environment (dev, staging, prod)
# ENVIRONMENT=dev

# Log level (DEBUG, INFO, WARNING, ERROR)
# LOG_LEVEL=INFO

# ===========================================
# How to Use
# ===========================================
#
# 1. Copy this file:
#    cp .env.example .env
#
# 2. Edit .env with your secure passwords
#
# 3. Create Kubernetes secrets:
#
#    # Load environment variables
#    source .env
#
#    # Create PostgreSQL secret
#    kubectl create secret generic postgresql-credentials \
#      --from-literal=postgres-password=$POSTGRES_PASSWORD \
#      --from-literal=password=$POSTGRES_USER_PASSWORD \
#      -n demo-platform
#
#    # Create Redis secret
#    kubectl create secret generic redis-credentials \
#      --from-literal=redis-password=$REDIS_PASSWORD \
#      -n demo-platform
#
# 4. Deploy with Argo CD (secrets must exist first!)
